- if Flipper.enabled?(:comments, current_user)
    .page-item.comments
        .grid
            = render commentable.comments.accessible_to(current_user)
            = render "comments/new", commentable: commentable