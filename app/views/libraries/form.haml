= render PageHeader.new(title: Library.model_name.human) 
= form_with model: @library, builder: CoachNotebookFormBuilder do |form|
    = form.text_field :name 
    = form.text_area :notes
    = form.submit
